# Assignments

Goal: Improve Story Mode into a usable NPC interaction experience with dialog, choices, animated NPC faces, and collectable scene items

Director-local work:
- Implement web UI and helper changes.
- Integrate tests and run verification.

Scout subagent:
- Read-only inspection of Story runtime/API contracts and frontend assumptions.
- Finding: current runtime already supports fixed choices and visible item pickup; frontend was the main gap.
