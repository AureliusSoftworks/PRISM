/**
 * Single source of truth for the product version string shown in the web UI.
 * Keep in sync with native MARKETING_VERSION / server installers / CHANGELOG.
 */
export const PRISM_APP_VERSION = "0.6.2";
