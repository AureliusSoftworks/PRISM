/**
 * Begin preparing the following turn late enough that an explicit live
 * interruption can still discard the speculative work cleanly.
 */
export const SIGNAL_FOLLOWING_TURN_PREFETCH_PROGRESS = 0.72;
