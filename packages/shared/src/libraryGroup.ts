/** Saved custom Library groups require at least two owned bots. */
export const BOT_LIBRARY_GROUP_MEMBER_MIN = 2;

/** Saved Library group membership is bounded for API, backup, and UI parity. */
export const BOT_LIBRARY_GROUP_MEMBER_MAX = 100;
